[build]
  command = "npm run build"
  publish = "dist/fpl-app/browser"

[build.environment]
  NODE_VERSION = "22"

# /api/* is handled by netlify/edge-functions/fpl-api-proxy.js

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
